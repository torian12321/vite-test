{
  "name": "lib-example",
  "private": true,
  "workspaces": [
    "packages/*",
    "sites/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "prettier": "lerna run prettier --stream",
    "prettier:check": "lerna run prettier:check --stream"
  },
  "devDependencies": {
    "@nx/vite": "18.0.1",
    "lerna": "^8.0.2",
    "nx": "18.0.1",
    "prettier": "^3.2.5",
    "pretty-quick": "4.0.0"
  },
  "dependencies": {
    "nx": "^18.0.1"
  }
}
