import type { FieldProps } from './Fields.types';
export declare const DropdownField: ({ type, name, value, label, loading, disabled, required, placeholder, properties, error, onChange, onBlur, }: FieldProps) => JSX.Element | null;
